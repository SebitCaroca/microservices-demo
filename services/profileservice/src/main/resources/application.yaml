spring:
  application:
    name: profileservice
  config:
    import: configserver:http://${CONFIG_HOST:localhost}:8888
  cloud:
    config:
      fail-fast: true
      retry:
        initial-interval: 2000
        max-interval: 10000
        multiplier: 1.5
        max-attempts: 20